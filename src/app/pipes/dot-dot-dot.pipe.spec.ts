import { DotDotDotPipe } from './dot-dot-dot.pipe';

describe('DotDotDotPipe', () => {
  it('create an instance', () => {
    const pipe = new DotDotDotPipe();
    expect(pipe).toBeTruthy();
  });
});
